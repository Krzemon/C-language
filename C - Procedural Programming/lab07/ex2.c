#include <stdio.h>

char** divide(char *str, int *len);
void cut(char *words);
char* join(char **words, int len);

int main(int argc, char* argv[]){

    char *zdanie = argv[1];
    // printf("%s\n", zdanie); 

    
       
    return 0;
}

char** divide(char *str, int *len);
void cut(char *words);
char* join(char **words, int len);